# G1 Root-Fit Summary

Result: local pass for the exact, unwired root primitives.

The decisive retained-host transition is:

```text
compact A 327 -> expanded B 471 -> compact A 327
```

The compact and canonical baseline measurements were invariant when the initial host allocation and fitting proposal were independently varied through 392, 472, 1,000, and 10,000 points. `preferredContentSize` and `sizeThatFits(in:)` agreed after half-point normalization.

Canonical short error, loading, and no-banner/no-Extra Game Over each measured exactly 320 × 392. Long error, both Claude-installed reconnect branches, recovering text, Extra Usage, every cached-banner reason, and variable Game Over fixtures all remained allocation-independent and used 392 as a minimum.

The test uses the packaged Press Start font, a fixed display clock/timezone, static synthetic animation frames, and explicit host teardown. The AppKit suite is opt-in because running synchronous hosting measurements in parallel with the repository's short-delay monitoring tests perturbs their timing. The normal parallel suite skips this suite and passes; the isolated gate command enables and passes all six AppKit tests.

This evidence selects the fixed-after-minimum root policy for continued characterization. It does not authorize the live AppDelegate cutover. The same matrix must run against the routed `UsagePopoverView` at I1, and the missing E0-R operating-system/display tuples remain release blockers.
